File-Tools
==========

This is Windows app which provides ways to search a file using a pattern search and rename file if a match is found. Another part of app is useful for finding all duplicate file in a folder and remove if user want to delete them. User can also see all files contained in a folder and its sub folder. Finding duplicate file is sometime error prone.
